# github pratice 

#concepts where to start gitaction yml file

cretae a folder name ".github/workflow/.yml"

# then write the whole steps that use in gitactions 

  #workglow - sholud have there name and when workflow run useing "on"
  
  #jobs - shold have inside the workflow  

  #steps- inside the jobs

  #action
